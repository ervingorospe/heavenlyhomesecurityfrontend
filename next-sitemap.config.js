/** @type {import('next-sitemap').IConfig} */

const config = {
  siteUrl: 'https://fascinating-croquembouche-171853.netlify.app',
  generateRobotsTxt: true, // (optional)
  // ...other options
}

module.exports = config